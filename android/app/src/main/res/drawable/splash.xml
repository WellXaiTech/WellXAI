<?xml version="1.0" encoding="utf-8"?>
<!-- Replaces the old leftover blue "WellX" splash bitmaps (from before the
     native Compose rewrite) with the real ChatGiZa mark: the same
     diagonal three-pill icon used as the actual live app/favicon icon
     (public/icons/icon-512.png in the web app), centered on the app's
     actual black background (colors.xml / styles.xml both pin this app to
     black, never the system's light default). A layer-list with a
     fixed-size, center-gravity item keeps the mark from stretching to fit
     the screen the way a plain full-bleed image background would. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/colorPrimary" />
    <item
        android:width="160dp"
        android:height="160dp"
        android:gravity="center">
        <bitmap
            android:src="@drawable/ic_chatgiza_logo"
            android:gravity="center" />
    </item>
</layer-list>
